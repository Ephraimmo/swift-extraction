{
  "name": "swift-extraction-workspace",
  "private": true,
  "scripts": {
    "dev": "bun --cwd swift-extraction run dev",
    "build": "bun --cwd swift-extraction install --frozen-lockfile && bun --cwd swift-extraction run build",
    "lint": "bun --cwd swift-extraction run lint"
  }
}